# 머니플래너

개인과 가족이 함께 쓰는 가계부 웹앱. 계좌·생활비·저축·대출·관리비를 다섯 탭으로 관리하고,
6자리 코드로 기기 사이를 실시간 동기화한다. 공개 서비스가 아니라 **기기 두세 대 규모의 개인용**이며,
주로 **아이폰 사파리에서 홈 화면에 추가해 앱처럼** 쓴다(화면 폭 375px, 손으로 누르는 조작이 전제).

**`index.html` 한 파일이 앱 전부다.** HTML·CSS·JavaScript가 그 안에 다 들어 있고, 빌드 도구·패키지
관리자·번들러·테스트 프레임워크가 **없다.** 외부 의존성은 CDN의 Supabase 클라이언트 하나뿐이다.
파일을 브라우저로 열면 그대로 동작하고, 정적 호스팅에 올리는 것이 곧 배포다.

## 이 저장소의 문서

```
money-planner/
├── CLAUDE.md                        ← 여기. 프로젝트 개요와 작업 전 확인 사항
├── AGENTS.md                        ← CLAUDE.md와 같은 내용 (다른 도구용 입구)
├── index.html                       ← 앱 전체. 유일한 소스 파일
└── docs/
    ├── architecture.md              ← 화면을 그리는 두 방식, 조작이 흐르는 길, 데이터 경로
    ├── business-rules.md            ← 탭별 계산식, 금액 부호, 날짜, 목록 순서, 이월 규칙
    ├── security.md                  ← 개인 탭 잠금이 지키는 선과 지키지 않는 선
    ├── standards.md                 ← 어기면 깨지는 규칙, 합치기 전 확인 게이트
    ├── engineering-notes.md         ← 몰라서 걸려 넘어지기 쉬운 것들
    ├── operations.md                ← 띄우는 법, 문법 검사, 확인용 데이터 넣는 법
    ├── contracts.md                 ← 백업 파일 형식, 클라우드 행 형식
    └── tracking/
        ├── status.md                ← 지금까지 된 것과 남은 일
        ├── findings.md              ← 아직 못 푼 문제
        └── decisions/               ← 다르게 고를 수 있었던 결정과 그 이유
```

모듈로 나뉘어 있지 않다. 소스가 `index.html` 하나이므로 모듈별 문서도 없다.

## 반드시 지켜야 하는 것

전체 규칙은 `docs/standards.md`에 있다. 그중 어기면 되돌릴 수 없는 것 네 가지.

1. **돈 기록을 지우거나 조용히 바꾸지 않는다.** 읽을 수 없는 값은 안전한 값으로 바꾸되 줄 자체는
   남긴다. 이미 저장된 값을 일괄 변환하지 않는다.
2. **입력 중에 화면을 통째로 다시 그리지 않는다.** `render()`가 `#mp-main`을 `innerHTML`로
   교체하므로, 사용자가 커서를 올려둔 입력칸이 새 노드로 바뀌어 아이폰에서 키패드가 내려간다.
   숫자가 바뀔 때는 항목별 부분 갱신 함수를 쓴다.
3. **`index.html` 한 파일과 무빌드를 유지한다.** 파일을 쪼개지 않고 새 외부 의존성을 넣지 않는다.
4. **확인 작업에서 실제 동기화 코드로 연결하지 않는다.** 파일에 실제 Supabase 주소와 키가 박혀
   있어, 연결하면 실제 가계부가 내려오고 그 뒤 저장이 사용자 휴대폰의 데이터를 덮어쓴다.

## 작업하기 전에

**항상 읽을 것**

- `docs/standards.md` — 특히 맨 아래 "바꾸지 않기로 정해진 것". 버그처럼 보이지만 사용자가
  그대로 두기로 정한 것들이 있다(저축 탭 집안행사비용이 더해지는 계산, 동기화 코드 6자리 등).
  고칠 이유가 생겼다면 사용자에게 먼저 묻는다.
- `docs/engineering-notes.md` — 확인 작업을 시작하기 전에. `file://`로 열면 저장이 아예 안 되고,
  저장소에 값을 넣고 새로고침하면 앱이 그 값을 덮어쓴다.

**무엇을 건드리느냐에 따라 먼저 읽을 것**

| 건드릴 것 | 먼저 읽기 |
|---|---|
| 금액·날짜 입력칸 | `business-rules.md`의 "금액 부호 — 전체 규칙"과 "날짜". 마이너스를 받는 칸은 개인 탭 기준 잔액 하나뿐이고, 날짜는 치는 도중 저장하지 않는다 |
| 데이터를 읽어들이는 곳 | `business-rules.md`의 "깨진 데이터를 다루는 규칙" + `contracts.md`. 세 경로가 `normalizeState()` 하나를 지나며, 모르는 필드가 살아남는 성질에 이월 짝 표식이 의존한다 |
| 목록 정렬 | `business-rules.md`의 "목록 순서". 정렬 기준값을 뺄셈으로 비교하면 예전 기록에서 비교가 망가진다 |
| 이월 | `business-rules.md`의 "이월" + `decisions/0003`. 두 줄이 한 쌍이고 `carryFrom`과 `carryPairId`는 다른 필드다 |
| 비밀번호·내보내기·가져오기 | `security.md`. 잠긴 범위는 "풀기 전까지 어느 탭에 있든"이고, 안내 화면의 가져오기만 예외다 |
| 동기화 | `security.md`의 "동기화 접근" + `contracts.md`의 "클라우드 행". 확인되지 않은 코드로 쓰면 남의 가계부가 사라진다 |
| 화면 그리기 | `architecture.md`의 "화면을 그리는 두 가지 방식" + `engineering-notes.md`. 아이폰 우회 코드는 이상해 보여도 이유가 있다 |

**고친 다음**

`docs/operations.md`의 문법 검사를 돌리고, 브라우저에서 **375px 폭으로** 직접 눌러 확인한다.
무엇을 눌렀고 무엇이 보였는지 남기고, 통과하지 못한 항목을 통과한 것처럼 적지 않는다.

## 문제를 발견했을 때

**바로 사용자에게 알릴 것** — 되돌릴 수 없는 것들이다.

- 돈 기록이 사라지거나 값이 조용히 바뀌는 경로를 찾았을 때
- 저장된 내용이 빈 상태로 덮이는 경로를 찾았을 때 (동기화가 켜져 있으면 다른 기기까지 덮인다)
- 확인되지 않은 동기화 코드로 쓰는 경로를 찾았을 때 (남의 가계부가 사라진다)
- 개인 탭 내용이 잠긴 상태에서 파일로 나가는 경로를 찾았을 때
- 사용자가 그대로 두기로 정한 것을 고쳐야 할 이유가 생겼을 때

그 밖의 것은 `docs/tracking/findings.md`에 남긴다. 다만 **먼저 풀어 본다** — 원인을 찾아 고칠 수
있으면 고치고, 알게 된 것은 `engineering-notes.md`에, 새 규칙이 필요하면 `standards.md`에 적는다.
findings는 "지금 자리에서 해결할 수 없는 것"을 두는 곳이지 "귀찮아서 미룬 것"을 두는 곳이 아니다.
